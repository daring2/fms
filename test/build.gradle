archivesBaseName = 'fms-test'

dependencies {
    compile "junit:junit:4.12"
    compile "org.mockito:mockito-core:2.7.12"
    compile "org.awaitility:awaitility:3.0.0"
    compile("io.kotlintest:kotlintest:2.0.5") {
        exclude module: "guava"
    }
}